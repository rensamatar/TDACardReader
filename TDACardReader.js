'use strict';
/**
 * This exposes the native TDA module as a JS module. This has a
 * function 'initLicense' which takes the following parameters:
 * bla bla
 */
var { NativeModules } = require('react-native');
module.exports = NativeModules.TDACardReader;